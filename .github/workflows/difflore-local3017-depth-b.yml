name: 'Difflore Local3017 Depth B'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    name: 'Depth proof B'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v6
      - name: 'Echo local3017 proof B'
        run: |
          echo 'local3017 depth proof B'
