name: 'Difflore Local3017 Depth C'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    name: 'Depth proof C'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v6
      - name: 'Echo local3017 proof C'
        run: |
          echo 'local3017 depth proof C'
