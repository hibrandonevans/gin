name: 'Difflore Local3017 Depth A'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    name: 'Depth proof A'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v6
      - name: 'Echo local3017 proof A'
        run: |
          echo 'local3017 depth proof A'
